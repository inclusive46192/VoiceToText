## Agent skills

### Issue tracker

GitHub Issues are used for tracking work in this repo. See `docs/agents/issue-tracker.md`.

### Triage labels

This repo uses the default triage labels: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: root-level domain docs and `docs/adr/` are the canonical references. See `docs/agents/domain.md`.
